"""The only place the two algorithms are allowed to differ.

Both implement the same ``Optimizer`` ABC and return the same ``RunResult``.
Beyond the optimizer loop and its genotype, they share everything.
"""
